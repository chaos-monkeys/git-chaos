name: Hello World
# Triggers on push or pull request
on: [push, pull_request]

jobs:
  echo:
    name: HelloWorld
    runs-on: ubuntu-latest
    steps:
      - name: comment PR
        uses: /chaos-monkeys/git-chaos/pr-comment
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          msg: "Hello World from Github Action!"
