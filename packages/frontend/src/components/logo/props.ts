export interface BuildLogoProps {
  boxStyle: string;
  headingStyle: string;
  text: any[];
}

export interface LogoProps {
  linkStyle: string;
  boxStyle: string;
  headingStyle: string;
  title: string;
  isLink?: boolean;
}
